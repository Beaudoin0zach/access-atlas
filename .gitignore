# build output
dist/
.output/
.vercel/
.netlify/

# deps
node_modules/

# astro
.astro/

# env — never commit secrets (§6 privacy). Only .env.example is tracked.
.env
.env.*
!.env.example

# supabase local
supabase/.branches/
supabase/.temp/

# playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# native (Capacitor) — the ios/ Xcode project is generated on a Mac via
# `npx cap add ios` and is regenerable; don't commit it (see docs/ios-testflight.md).
/ios/

# os
.DS_Store
